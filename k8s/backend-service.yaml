apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: microservices
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP